# CMF Phone 1 (A015 / Tetris, mt6878) - earpiece as second speaker
#
# Verified working on Nothing OS B4.1-260615-1652, Android 16.
#
# Why these three, in this order:
#
# On this device the media path never touches the internal codec. Playback
# goes DL_24CH -> I2SOUT4 -> Awinic aw_dev_0 smart PA -> bottom speaker. The
# earpiece hangs off the internal codec's DAC (ADDA_DL), which normally has
# no source connected at all - which is why simply pointing RCV Mux at the
# receiver produces silence.
#
#   1. Handset Volume            earpiece gain, set BEFORE it is driven
#   2. ADDA_DL_CH1 DL_24CH_CH1   feed the internal DAC from the same stream
#                                that is already going to the speaker
#   3. RCV Mux = Voice Playback  route that DAC to the earpiece
#
# Order matters: connect the feed before opening the mux, so the receiver is
# never pointed at an undefined path.

ctl|Handset Volume|31
ctl|ADDA_DL_CH1 DL_24CH_CH1|1
ctl|RCV Mux|Voice Playback

# ---- gain -------------------------------------------------------------
# Handset Volume advertises "range 0->18", but that ceiling is misdeclared:
# the HAL's own default for this control is 31, as are its Headset and
# Lineout Volume settings. 31 is therefore a vendor-sanctioned value, and it
# is where this sits.
#
# 31 is also the hardware ceiling. The register field is 5 bits, so 40 wraps
# to 8 and gets quieter rather than louder - measured on the device, not
# assumed. There is no headroom above 31 to find.
#
# Lower it if you prefer; do not raise it. The receiver has no protection
# circuit or excursion limit, and that damage is permanent - it also costs
# you the earpiece for phone calls.
#
# ADDA_DL_GAIN is deliberately NOT set here. The HAL raises it to ~63311 of
# 65535 on its own when a stream starts, and forcing it to maximum would also
# raise headphone output, which shares the ADDA path.
#
# ---- channels ---------------------------------------------------------
# As written, the earpiece gets the LEFT channel (DL_24CH_CH1) while the
# speaker still gets both. That is a widened image, not a true split. For a
# real split the speaker has to be reduced to the right channel by turning
# off its left feed:
#
#   ctl|I2SOUT4_CH1 DL_24CH_CH1|0
#
# Only add that line if the speaker is confirmed to take its audio from
# CH2 - on a single-amp phone it may be wired to CH1, in which case that
# line silences the speaker entirely.
